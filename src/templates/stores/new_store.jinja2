{% extends "base.jinja2" %}

{% block content %}
    <div class="page-header">
        <h1>Create Store</h1>
    </div>
    <div class="form-size">
        <form method="post">
            <div class="form-group">
                <label for="name">Store Name</label>
                <input type="text" class="form-control" id="name" name="name" placeholder="Input Store name...">
            </div>
            <div class="form-group">
                <label for="url_prefix">Store URL Prefix</label>
                <input type="text" class="form-control"
                       id="url_prefix" name="url_prefix"
                       placeholder="Input Store URL Prefix: http://..."
                       aria-describedby="storeURLPrefixHelp">
                <span id="storeURLPrefixHelp" class="help-block">What do the URLs in the online store begin with?</span>
            </div>
            <div class="form-group">
                <label for="tag_name">Tag name</label>
                <input type="text" class="form-control"
                       id="tag_name" name="tag_name" value="span"
                       placeholder="Input Store tag name: span..."
                       aria-describedby="storeTagName">
                <span id="storeTagName" class="help-block">What's the HMTL tag of the item that contains the item price? (usually <code>span</code>)</span>
            </div>
            <div class="form-group">
                <label for="query">Search Query</label>
                <input type="text" class="form-control"
                       id="query" name="query" value='{"id":"price-now"}'
                       placeholder='Input the query: {"id":"price-now"}'
                       aria-describedby="queryHelpBlock">
                <span id="queryHelpBlock" class="help-block">
                    What JSON query would we have to do to find the specific tag with the price? (Remember to use double quotation marks!)
                </span>
            </div>
            <button type="submit" class="btn btn-default">Create</button>
        </form>
    </div>
{% endblock %}